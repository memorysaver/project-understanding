# Lessons: PL-015 (console-api)

Module: packages/api (console surface)
Activity: define-voice (active StylePrompt read/update)
Date: 2026-06-23
Story: PL-015

## Solutions

## Errors

## Missing

## Summary for Next Agent

Straightforward backend-only oRPC story — no surprises. Compose console procedures
on `protectedProcedure` (PL-014) so the 401 gate lives in one place; do not
re-implement auth per procedure. For single-active `style_prompts`, an in-place
transactional update of the `is_active = true` row is the simplest path that holds
the PL-001 invariant (no second active row, no 0/2-active window). Test harness:
mirror `auth.test.ts` — in-memory `bun:sqlite` + the real PL-001 migration with an
injected oRPC context; never touch the prod `createContext`. Build resumed cleanly
from an orphaned Phase-5 session: rebase onto current main was clean (infra PRs
#16/#17 don't touch packages/api), and the separate-evaluator gen/eval loop passed
on round 1.

> Resume/process note: this repo has no `.github/workflows/` — there are no CI
> checks, so "no checks reported" on a PR is the expected green state, not a
> failure. `gh pr merge --delete-branch` errors on the local-branch-switch step
> because `main` is held by the main worktree; the remote squash-merge still
> succeeds — delete the remote branch separately with `git push origin --delete`.
