# Layer 1 gate — Console + manual ingest (in progress)

> Evidence for `layer_gates[1]` in `product-context.yaml` (paperlens). The gate is **two-phase**:
> `scripted_passed` (Tier-1 framework tests green) → `passed` (all applicable tiers green **+** every
> acceptance criterion proven **+** prior-layer journeys replay green). `/aep-wrap` reads this file before
> flipping. This file accrues evidence per Layer-1 story; PL-020 is recorded below.

**Status:** `not_started` → `scripted_passed` (this story)   ·   **Date:** 2026-06-26   ·   **Run by:** aep-build (PL-020)

## Coverage summary (PL-020 slice)

- **Criteria proven (PL-020):** 2 / 2
- **Tiers (applicable only):** Tier-1 scripted `PASS` · Tier-2 journey `DEGRADED→Tier-3` (live workerd dev server unreachable from the sandboxed shell; proven at the integration layer through the real procedure + real auth middleware) · Tier-3 API `PASS` (integration)
- **Regression:** prior-layer journeys replay `N-A` (Journey 00 is a `<…>` seed; Layer-0 gate is conditional_pass via the faithfulness eval, not a UI journey)

## Acceptance traceability (PL-020)

| Acceptance criterion                                              | Proving test                                                                 | Verify (what confirms it)                                                                                          | PASS/FAIL |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | --------- |
| PL-020-ac1: `triggerRun` enqueues a discovery message + creates a Run row | Journey 20 · scenario 20.1 · scripted `run.test.ts` 5.2                        | exactly 1 `runs` row (`trigger="manual"`) + exactly 1 recorded `{type:"discover",runId}` whose runId == returned runId | PASS      |
| PL-020-ac2: a console button invokes it; reader side cannot       | Journey 20 · scenario 20.2 · scripted `run.test.ts` 5.1 + route/registration audit | unauth `triggerRun` → 401, 0 Run rows, empty queue (handler-side sentinel); `/console` `beforeLoad` redirects to `/login`; `triggerRun` registered only on `protectedProcedure` | PASS      |

## Scripted-coverage matrix (Tier-1) — PL-020

| Case (test name)                                                                                  | Asserts (invariant)                                                                 | PASS/FAIL |
| ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------- |
| `triggerRun — without a session throws 401 (UNAUTHORIZED)`                                        | unauthenticated call is rejected with ORPCError UNAUTHORIZED / 401                   | PASS      |
| `triggerRun — does not create a Run row or enqueue a message when unauthenticated`                | fail-closed: 0 Run rows + empty recording queue on 401                              | PASS      |
| `triggerRun — the auth gate runs before the handler (sentinel stays false on denial)`             | the gate short-circuits BEFORE the handler body (no side effect)                    | PASS      |
| `triggerRun — creates exactly one manual Run row and enqueues exactly one matching discover message` | delegates to enqueueDiscovery: 1 `trigger="manual"` row + 1 `discover` msg, runId match | PASS      |
| `triggerRun — returns { runId: string }`                                                          | result contract shape pinned                                                        | PASS      |

## Manual dogfood checklist (Tier-2/3)

- [x] Tier-3 (integration, degraded from live): unauthenticated `triggerRun` → 401, no Run row, no enqueue — proven via `run.test.ts` through the real `protectedProcedure` middleware + in-memory db + recording queue. See `.dev-workflow/dogfood-PL-020.md`.
- [x] Tier-3 (integration): authenticated `triggerRun` → 1 manual Run row + 1 matching `discover` message — proven via `run.test.ts`.
- [ ] Live browser Journey 20 against `$BASE_URL` — DEFERRED: the workerd dev server is not reachable from the sandboxed shell (HTTP 000 on all paths despite "Ready" in the log). `WAIVER` below.

## Findings

No defects. See `.dev-workflow/dogfood-PL-020.md` for the full dogfood report (method note + coverage matrix).

## Waivers

`WAIVER: live-browser Journey 20 (Tier-2)` — the local Alchemy/workerd dev server reports "Ready" but is
unreachable via curl from the sandboxed build shell (HTTP 000 on every path including the server root), so a
live UI/RPC dogfood could not run here. The same code path (real `triggerRun` + real `protectedProcedure`
auth middleware + real db + recording queue) is proven at the integration layer (Tier-1/Tier-3-equivalent),
and the web button is validated by `check-types` + the production build (the `/console/` route chunk
compiled). The codified Journey 20 stands as the regression artifact to run live once the dev-server
reachability is resolved (or at a `deployed:` target).

## Evidence

- **API JSON / logs:** `.dev-workflow/dogfood-PL-020.md`
- **Scripted suite:** `packages/api/src/routers/run.test.ts` (5 tests) → `layer_gates[1].evidence.scripted`
- **Journeys:** Journey 20 (`skills/e2e-test/journeys/20-run-now-trigger.md`) → `layer_gates[1].evidence.journeys`
- **Evaluator:** Phase 5 separate-evaluator verdict PASS, 0 FAIL items (`.dev-workflow/signals/eval-response-1.md`)
